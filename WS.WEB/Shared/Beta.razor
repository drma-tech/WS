<MudAlert Severity="Severity.Warning" Variant="Variant.Outlined" Dense="true" Class="@Css.Build().Small("my")">
    <span class="me-2" style="font-weight: bold;">Beta:</span>@GlobalTranslations.Beta
</MudAlert>

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();

        AppStateStatic.BreakpointChanged += breakpoint => StateHasChanged();
        AppStateStatic.BrowserWindowSizeChanged += size => StateHasChanged();
    }
}